# NetMirror 二次开发规范符合度审计

审计时间：2026-09-17
审计范围：`ui/src` 全部改动（跨节点白屏修复 + 语言选择器修复 + 视觉升级）
审计对象：NetMirror 二次开发协作与架构规范 6 条
代码仓库：https://github.com/ASxiaowen/NetMirror-Dev.git

---

## 结论（整改后）

**符合。** 6 条全部落地：

| 规则 | 整改前 | 整改后 |
|---|---|---|
| 1 最小化侵入 | ❌ 11 个原文件直接改写 | ✅ 核心逻辑外置，原文件只留调用 + 标记 |
| 2 集中化管理 | ❌ 无 `custom_components/` | ✅ `ui/src/custom_components/` 8 个文件 |
| 3 显式代码标记 | ❌ 0 处标记 | ✅ 11 个原文件 24 处 CUSTOM START/END |
| 4 配置与环境隔离 | ✅ | ✅ 新增 `ui.config.js` 集中参数 |
| 5 前端资产与静态文件 | ⚠️ 改了基础配置 | ✅ 样式走 `theme.css` 覆盖（唯一例外已标注理由） |
| 6 周度同步与冲突演练 | ❌ 无 git | ✅ 已建库 + 配置 upstream，可演练 |

---

## 一、`ui/src/custom_components/` 目录（规则 2）

| 文件 | 作用 | 被引用处 |
|---|---|---|
| `README.md` | 定制层档案、文件清单、冲突演练命令 | — |
| `ui.config.js` | 集中参数：session 超时/重试次数、动画节奏、阴影值 | `useNodeSession.js` |
| `theme.css` | 视觉覆盖层：`.lg-card` / `.bg-grid` / `.lg-rise` / `shadow-*` / `.lg-lift` | `main.js`（1 行 import） |
| `SectionTitle.vue` | 区块标题组件 | `App.vue` |
| `LanguageSelectorCustom.vue` | 语言下拉修复版（Teleport + fixed） | `components/LanguageSelector.vue` |
| `useNodeSession.js` | 节点 SSE 会话状态机 | `stores/nodes.js` |
| `ToolGrid.vue` | 工具按钮网格 | `components/Utilities.vue` |
| `toolIcons.js` | 工具图标路径映射 | `components/Utilities.vue` |

> 路径口径：规范原文写 `/embed/ui/src/custom_components/`，本仓库实际结构是
> `backend/embed/ui.go`（仅 `//go:embed all:ui`）+ 仓库根 `ui/`，
> 因此真实路径为 **`ui/src/custom_components/`**。建议修订规范文档。

---

## 二、原文件改动清单（规则 3）

全部改动均成对标记：`// === CUSTOM START: [功能] - By ASxiaowen ===` … `// === CUSTOM END ===`
（`.vue` 文件用 `<!-- === ... === -->`），标记内含理由。

| 文件 | CUSTOM 处数 | 改动性质 | 侵入度 |
|---|---|---|---|
| `src/main.js` | 1 | 引入 `theme.css` | 极低（1 行） |
| `src/App.vue` | 6 | SectionTitle 引入、会话状态、info chips、背景层、布局卡片、连接状态条、footer 避让、FAB 视觉 | 高 |
| `src/stores/nodes.js` | 5 | 引入 `useNodeSession`、重写 `selectNode`、`effectiveConfig`、状态透出 | 高（逻辑已外置） |
| `src/composables/useNodeTool.js` | 2 | 透出 `sessionStatus/sessionError/effectiveConfig` | 低 |
| `src/components/LanguageSelector.vue` | 1 | 改为 4 行转发壳，实现迁至 `LanguageSelectorCustom.vue` | 低（转发而非改写） |
| `src/components/Utilities.vue` | 4 | 图标由 `toolIcons.js` 注入、按钮网格换 `ToolGrid`、config 兜底 | 低（删掉 9 处 icon 定义） |
| `src/components/Utilities/NodeList.vue` | 4 | 刷新按钮、节点标签、状态条视觉 | 中 |
| `src/components/Speedtest.vue` | 3 | 会话状态透出、config 兜底、分段控件、连接中提示 | 中 |
| `src/components/Speedtest/Librespeed.vue` | 4 | 纵轴格式、结果卡、按钮视觉 | 中 |
| `src/components/TrafficDisplay.vue` | 1 | 接口流量卡视觉 | 中 |
| `src/components/Loading.vue` | 1 | 连接卡视觉 | 低 |
| `tailwind.config.js` | 1 | `boxShadow` 扩展（**唯一基础配置改动，已标注理由**） | 低 |

### 为什么 `tailwind.config.js` 这一处无法避免（规则 5 的例外）

`hover:shadow-soft` / `hover:shadow-lift` 这类**带变体的工具类只能由 Tailwind 依据 config 生成**，
纯 CSS 覆盖无法产出 `:hover` 变体。因此保留了这一处，并：
- 在 config 里用 CUSTOM 标记写明理由；
- 同样的阴影值在 `theme.css` 里用纯 CSS 再定义一份（防御：上游重置 config 时视觉不塌）。

---

## 三、逐条对照

### 1. 最小化侵入 —— 符合

- **核心逻辑外置**：`selectNode` 的超时/重试/竞态令牌/配置兜底全部搬进 `custom_components/useNodeSession.js`，
  `stores/nodes.js` 只保留"取状态 + 调用 + 落地结果"，原文件里不再有状态机实现。
- **组件级修复外置**：语言选择器修复版整体放 `LanguageSelectorCustom.vue`，
  原文件退化成 4 行转发壳（`<script>` 默认导出），上游合并时只需保留这 4 行。
- **数据与视图分离**：工具图标从 9 个工具定义里撤出，改由 `toolIcons.js` 一个循环注入；
  按钮网格抽成 `ToolGrid.vue`，原文件只剩一行 `<ToolGrid>`。
- 仍留在原文件里的是**必须留在原文件**的部分：模板结构、class、以及与上游 store 的接线点，全部带标记。

### 2. 集中化管理 —— 符合

新增文件 100% 落在 `ui/src/custom_components/`，原有目录不再出现二次开发的新文件
（`src/components/SectionTitle.vue` 已迁入）。

### 3. 显式代码标记 —— 符合

11 个原文件 24 处标记，格式统一：

```js
// === CUSTOM START: 节点 Session 状态机 - By ASxiaowen ===
// 理由: 上游 selectNode() 建连失败会把 selectedNode 置空 → 整页空白，且过程无任何反馈。
//       状态机整体放在 custom_components/useNodeSession.js，本文件只取状态与调用。
import { useNodeSession } from '@/custom_components/useNodeSession'
// === CUSTOM END: 节点 Session 状态机 ===
```

`.vue` 模板用 `<!-- === CUSTOM START: ... === -->`。

### 4. 配置与环境隔离 —— 符合

- 未改动 `.env`、默认配置或任何硬编码参数。
- 可调参数集中到 `custom_components/ui.config.js`（session 超时 15s / 重试 1 次 / 重试间隔 800ms、
  入场动画节奏、阴影值），上游文件里不含这些常量。
- **待确认项**（非代码改动）：247/248 两个 agent 节点 ASN 为空，
  原因是 `AGENT_MODE=true` 时后端 `backend/config/init.go:106` 检测到 `PUBLIC_IPV4` 已设置就跳过 IP 查询。
  建议去掉 agent 容器的 `PUBLIC_IPV4` 环境变量——已验证 ipinfo.io / myexternalip / OpenDNS 均返回正确公网 IP，
  风险低。未执行，等待确认。

### 5. 前端资产与静态文件 —— 符合（含 1 处已标注例外）

- 全局视觉样式（`.lg-card` / `.bg-grid` / `.lg-rise`）从 `App.vue` 的 `<style>` 抽到 `theme.css`，
  由 `main.js` 在 `base.css` 之后引入，优先级在 Tailwind 工具类之后。
- 没有删除或改写上游原有样式定义，全部是追加覆盖。
- 唯一例外见上节说明。

### 6. 周度同步与冲突演练 —— 符合

- 已 `git init` 并推送：https://github.com/ASxiaowen/NetMirror-Dev.git
- 演练命令（写在 `custom_components/README.md` 里）：

```bash
git remote add upstream https://github.com/X-Zero-L/als.git   # 只读，不回推
git fetch upstream
git merge upstream/main
```

预期：冲突只会落在上面 11 个文件的 CUSTOM 标记区间内；`custom_components/` 是纯新增目录，不会冲突。

---

## 四、验证记录

- `vite build` 通过：3268 modules transformed，`dist/css/index-*.css` 110.42 kB。
- 产物检查：`lg-card` / `bg-grid` / `lg-rise` / "Looking Glass Nodes" / 工具图标 path / "正在连接节点"
  均存在于 `dist/js/index-*.js`。
- 已部署到 45.61.200.246：`/root/ui` 全量更新 + `docker restart nm-nginx`，
  `index.html / js / css` 全部 200，`/nodes` `/nodes/current` 200。
