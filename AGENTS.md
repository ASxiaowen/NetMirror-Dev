# 🛠️ NetMirror 二次开发协作与架构规范

为了确保本项目在二次开发过程中，能够**最大程度降低未来同步原仓库更新的冲突率**，并保持代码架构的清晰与可维护性，所有参与开发的成员必须严格遵守以下规范：

## 1. 核心原则：最小化侵入 (Minimal Invasion)
* **禁止大面积修改原代码**：严禁在原项目的核心逻辑文件（如主路由、核心控制逻辑、底层服务文件）中直接进行大规模的重构或业务逻辑叠加。
* **钩子/组合模式优先**：如果必须在原文件插入自定义逻辑，请使用**“注册回调”**或**“钩子（Hook）”**的方式。原文件只做极简的条件判断或函数调用，核心逻辑交由我们独立的模块处理。

## 2. 目录结构：集中化管理 (Centralized Storage)
所有二次开发新增的功能、业务逻辑、工具函数和自定义页面，必须统一放置在指定的**专属目录**中，禁止散落在原项目目录中。
* **后端独立目录**：所有新增的后端逻辑、API 接口、模型统一存放在 `/backend/custom_modules/` 下。
* **前端独立目录**：所有新增的 UI 组件或静态资源统一存放在 `/embed/ui/src/custom_components/` 下。
* **入口隔离**：仅允许在原项目的路由入口文件（如路由、主入口）中添加一行导入/注册语句，将我们的专属目录挂载进去。

## 3. 冲突防御：显式代码标记 (Explicit Tagging)
如果遇到极端情况，**不得不**直接修改原项目的现有文件或现有函数，必须在修改位置加上格式统一的**注释标记**。这在未来 Git 合并冲突时能提供极大的辨识度：
```text
// === CUSTOM START: [功能名称] - By [开发者姓名] ===
// 理由: 解释为什么要在这里修改原文件
你的二次开发代码...
// === CUSTOM END: [功能名称] ===
```

## 4. 配置与环境隔离 (Environment Isolation)
* **禁止修改默认配置文件**：不要直接在原项目的 `.env.example` 或默认配置文件中硬编码你的私密参数。
* **本地与生产分离**：创建专属的配置覆盖文件。确保所有由于二次开发引入的数据库连接、第三方 Token 等配置都通过环境变量或独立文件读取。

## 5. 前端资产与静态文件规范 (Frontend Assets)
由于此项目包含 `embed/ui`（前端部分），前端打包往往容易覆盖文件：
* **禁止直接修改原图标和基础样式表**：如果需要定制主题，请在全局样式表末尾通过 CSS 覆盖（Override），或者在专属目录中编写新的样式。
* **独立打包/按需引入**：确保新增的前端组件不会破坏原项目的构建流程，尽量采用异步加载或独立模块引入。

## 6. 定期同步与冲突演练 (Upstream Sync)
* **周度同步**：开发负责人需在每周固定时间，尝试将原作者仓库（Upstream）的最新分支拉取并合并到我们的开发分支，提早暴露并解决由于原作者更新导致的 API 变更或破坏性改动。
